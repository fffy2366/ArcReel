interface:
  display_name: "AFK Team Workflow"
  short_description: "Run an issue batch to merged or shelved"
  default_prompt: "Use $afk-team-workflow to run a Spec or issue batch unattended."
policy:
  allow_implicit_invocation: false
